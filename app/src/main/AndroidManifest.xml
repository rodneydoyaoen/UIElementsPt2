<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uielementspt2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UIElementsPt2">
        <activity android:name=".EditAlbumActivity"></activity>
        <activity android:name=".AlbumOptionsActivity" />
        <activity android:name=".AddAlbumActivity" />
        <activity android:name=".AlbumsTableHandler" />
        <activity android:name=".EditSongsActivity" />
        <activity android:name=".AddSongsActivity" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".AlbumActivity" />
        <activity android:name=".QueuedSongsActivity" />
        <activity android:name=".AlbumDetailsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>